# Explainable Resume Screening

This project implements a semantic resume–job description matching system using transformer-based embeddings.
It serves as the baseline for building explainable AI (XAI) techniques such as SHAP-based token attribution,
counterfactual analysis, and bias detection.